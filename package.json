{
  "type": "module",
  "name": "mqr-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Live performance dashboard for MQR April 2026 — Google Sheet + Meta API → Vercel.",
  "scripts": {
    "dev": "npx vercel dev --listen 3001",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "googleapis": "^144.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
