node_modules/
.env
.env.local
.env.*.local
.vercel
.DS_Store
*.log
dist/
.cache/
.claude/

# Word doc temp lock files
~$*

# Debug / scratch dumps
response.json
*.tmp

# Keep the .env.example (it's safe — no real values)


.env*.local

# Local env files without leading dot
env.local
env.*.local
